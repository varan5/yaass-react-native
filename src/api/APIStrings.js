// Apps base backend url
const baseURL = 'api.yaass.com/v1'

const APIStrings = {
  // some dummy apis
  isUserAuthenticated: baseURL + 'auth/isUserAuthenticated',
  getAllPosts: baseURL + 'getAllPosts',
}

export default APIStrings
